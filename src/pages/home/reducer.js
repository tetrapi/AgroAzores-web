import {
} from "./actions";

const initialState = {
};

export const dashboardReducer = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};